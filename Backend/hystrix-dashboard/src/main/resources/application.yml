server:
  port: 9008
spring:
  application:
    name: hystrix-dashboard
hystrix:
  dashboard:
    proxy-stream-allow-list:
      - "*"

management:
  endpoints:
    web:
      exposure:
        include: "*"
      basePath: "/"

